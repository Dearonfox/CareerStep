import os
from dotenv import load_dotenv

load_dotenv()

# ============================================================
# 전북대 컴퓨터인공지능학부 게시판 코드 매핑
# ============================================================
BASE_URL = "https://csai.jbnu.ac.kr"

BOARDS = {
    "취업정보": "4930",
    "일반공지": "4926",
    "학과소식": "4927",
}

HEADERS = {
    "User-Agent": (
        "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
        "AppleWebKit/537.36 (KHTML, like Gecko) "
        "Chrome/120.0.0.0 Safari/537.36"
    )
}

# 대외활동이 아닌 행정/학사 공지성 게시글을 걸러내기 위한 제외 키워드
# (휴복학, 수강신청, 시간표, 졸업, 학사일정 등 — 일반공지/학과소식 게시판에 많이 섞여 있음)
EXCLUDE_KEYWORDS: list[str] = [
    "휴학", "복학", "수강신청", "수강정정", "시간표", "졸업", "졸업유예",
    "학사일정", "등록금", "수업일수", "정원", "전공", "전입", "전출",
    "재입학", "제적", "강의평가", "성적", "휴강", "보강", "방학중",
    "코로나", "휴관", "휴업", "근로계약", "근로장학생", "조교 모집",
    # 이미 종료된 행사의 수상/성과 발표 글 (지원/참가 가능한 공고가 아님)
    "수상", "우수상", "최우수상", "대상 수상", "시상식", "시상",
    "동상", "은상", "금상",
    # 교수진 연구 성과 안내 (학생 대상 대외활동이 아님)
    "교수팀", "학술대회 발표", "논문 게재",
    # 학위/논문 관련 행정 공지
    "학위논문", "학위수여식", "심사 계획", "원문제출",
]

# 키워드 기반 카테고리 분류 (우선순위 순서대로 매칭)
# "채용공고"는 별도 일반 크롤러(JobKorea)에서 이미 수집하므로 여기서는 제외하고,
# 순수 대외활동(인턴십/공모전/캠프/교육/장학금/봉사/네트워킹/서포터즈 등)만 분류한다.
CATEGORY_RULES: list[tuple[str, list[str]]] = [
    ("인턴십", ["인턴", "intern"]),
    ("공모전", ["공모전", "공모", "대회", "챌린지", "해커톤", "경진", "아이디어", "contest", "competition", "hackathon"]),
    ("캠프/특강", ["캠프", "특강", "세미나", "강연", "강의", "워크샵", "워크숍", "컨퍼런스", "workshop", "conference", "camp", "부트캠프"]),
    ("교육/프로그램", ["교육", "프로그램", "과정", "수료", "training", "커리큘럼", "멘토", "교환학생", "연수", "글로벌", "산학협력", "현장실습"]),
    ("장학금", ["장학", "scholarship"]),
    ("봉사활동", ["봉사", "자원봉사", "캠페인", "volunteer"]),
    ("서포터즈/홍보대사", ["서포터즈", "홍보대사", "체험단", "ambassador"]),
    ("취업연계행사", ["설명회", "잡페어", "job fair", "박람회", "채용상담", "커리어"]),
    ("교류/네트워킹", ["네트워킹", "교류회", "MT", "networking"]),
]

# 기본 설정값
MAX_PAGES = None  # None이면 게시판 전체 페이지 수집
CRAWL_DELAY = 1.0  # 요청 간 지연 시간 (초)
RECRUIT_LOOKBACK_DAYS = 60  # 이 기간(일)보다 오래된 게시글은 수집하지 않음 (이미 마감된 공고 제외)

# 파일 저장 경로
DATA_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "data")
OUTPUT_FILE = os.path.join(DATA_DIR, "activities.json")

# MongoDB 설정
MONGODB_URI = os.getenv("MONGODB_URI")
